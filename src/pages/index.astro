---
const postModules = (import.meta as any).glob('../content/blog/*.md', { eager: true });
const recentPosts = Object.values(postModules)
  .filter((p: any) => p.frontmatter?.draft !== true)
  .sort((a: any, b: any) => new Date(b.frontmatter.pubDate).valueOf() - new Date(a.frontmatter.pubDate).valueOf())
  .slice(0, 3) as any[];
---
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SEO Wiesbaden | SEO Agentur Wiesbaden – Suchmaschinenoptimierung</title>
  <meta name="description" content="SEO Wiesbaden — Ihre SEO Agentur vor Ort. Wir steigern Ihre organische Sichtbarkeit mit nachweisbarer Suchmaschinenoptimierung im Rhein-Main-Gebiet." />
  <meta name="keywords" content="SEO Wiesbaden, Suchmaschinenoptimierung Wiesbaden, SEO Agentur Wiesbaden, Local SEO Wiesbaden" />
  <meta name="robots" content="index, follow" />
  <link rel="canonical" href="https://seo-wiesbaden.de/" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://seo-wiesbaden.de/" />
  <meta property="og:title" content="SEO Wiesbaden | SEO Agentur Wiesbaden" />
  <meta property="og:description" content="Professionelle Suchmaschinenoptimierung in Wiesbaden. Messbare Ergebnisse, persönliche Betreuung." />

  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "ProfessionalService",
    "name": "SEO Wiesbaden — SEO Agentur Wiesbaden",
    "description": "Professionelle Suchmaschinenoptimierung für Unternehmen in Wiesbaden und dem Rhein-Main-Gebiet",
    "url": "https://seo-wiesbaden.de",
    "priceRange": "€€",
    "areaServed": ["Wiesbaden", "Mainz", "Frankfurt", "Rhein-Main"],
    "knowsAbout": ["SEO", "Suchmaschinenoptimierung", "Local SEO", "Content Marketing", "Technisches SEO"]
  }
  </script>
</head>
<body>

<!-- NAV -->
<nav id="nav">
  <div class="nav-inner">
    <a href="/" class="logo" aria-label="SEO Wiesbaden Startseite">
      <span class="logo-rank">SEO </span><span class="logo-rhein">Wiesbaden</span>
    </a>
    <ul class="nav-links">
      <li><a href="#leistungen">Leistungen</a></li>
      <li><a href="#prozess">Ablauf</a></li>
      <li><a href="#ergebnisse">Ergebnisse</a></li>
      <li><a href="/blog">Blog</a></li>
    </ul>
    <a href="#kontakt" class="nav-cta">Analyse anfragen</a>
    <button class="nav-burger" aria-label="Menü öffnen" id="burger">
      <span></span><span></span><span></span>
    </button>
  </div>
  <div class="mobile-menu" id="mobileMenu">
    <a href="#leistungen">Leistungen</a>
    <a href="#prozess">Ablauf</a>
    <a href="#ergebnisse">Ergebnisse</a>
    <a href="/blog">Blog</a>
    <a href="#kontakt" class="mobile-cta">Kostenlose Analyse anfragen</a>
  </div>
</nav>

<!-- HERO -->
<section id="hero">
  <div class="hero-bg-word" aria-hidden="true">SEO</div>
  <div class="hero-grid" aria-hidden="true"></div>
  <div class="hero-inner">
    <div class="hero-tag">
      <span class="tag-line"></span>
      SEO Agentur Wiesbaden
    </div>
    <h1 class="hero-headline">
      <span class="hl-1">SEO Wiesbaden.</span>
      <span class="hl-2">Gefunden werden.</span>
      <span class="hl-3">Egal ob KI oder organisch.</span>
    </h1>
    <p class="hero-sub">
      Wir bringen Ihr Unternehmen auf Seite 1. Mit nachweisbarer
      Suchmaschinenoptimierung, die echte Kunden anzieht und Umsatz steigert.
    </p>
    <div class="hero-actions">
      <a href="#kontakt" class="btn-primary">Kostenlose SEO-Analyse</a>
      <a href="#ergebnisse" class="btn-ghost">Ergebnisse ansehen →</a>
    </div>
  </div>
  <div class="hero-scroll" aria-hidden="true">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<!-- STATS -->
<section id="stats" aria-label="Kennzahlen">
  <div class="stats-inner">
    <div class="stat">
      <span class="stat-n">8<sup>+</sup></span>
      <span class="stat-l">Jahre Erfahrung</span>
    </div>
    <div class="stat-div" aria-hidden="true"></div>
    <div class="stat">
      <span class="stat-n">120<sup>+</sup></span>
      <span class="stat-l">Kunden betreut</span>
    </div>
    <div class="stat-div" aria-hidden="true"></div>
    <div class="stat">
      <span class="stat-n">340<sup>%</sup></span>
      <span class="stat-l">Ø Traffic-Steigerung</span>
    </div>
    <div class="stat-div" aria-hidden="true"></div>
    <div class="stat">
      <span class="stat-n">1.200<sup>+</sup></span>
      <span class="stat-l">Seite-1-Rankings</span>
    </div>
  </div>
</section>

<!-- LEISTUNGEN -->
<section id="leistungen">
  <div class="section-wrap">
    <header class="section-header fade-up">
      <span class="sec-num" aria-hidden="true">01</span>
      <div>
        <p class="sec-tag"><span class="tag-line"></span>Was wir tun</p>
        <h2 class="sec-title">SEO-Leistungen für<br/>Wiesbadener Unternehmen</h2>
      </div>
    </header>

    <div class="services-grid">
      <article class="svc-card fade-up">
        <span class="svc-num" aria-hidden="true">01</span>
        <div class="svc-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
          </svg>
        </div>
        <h3>SEO-Strategie &amp; Beratung</h3>
        <p>Fundierte Keyword-Analyse, Wettbewerbsanalyse und ein individueller SEO-Plan. Zugeschnitten auf Ihren Markt in Wiesbaden und dem Rhein-Main-Gebiet.</p>
        <ul class="svc-list">
          <li>Keyword-Recherche &amp; Mapping</li>
          <li>Wettbewerbs- und Potenzialanalyse</li>
          <li>Technischer SEO-Audit</li>
        </ul>
      </article>

      <article class="svc-card fade-up" style="animation-delay: 0.1s">
        <span class="svc-num" aria-hidden="true">02</span>
        <div class="svc-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7z"/>
            <circle cx="12" cy="9" r="2.5"/>
          </svg>
        </div>
        <h3>Local SEO Wiesbaden</h3>
        <p>Lokal gefunden werden. Mit gezielter Local-SEO-Optimierung erscheint Ihr Unternehmen prominent in Google Maps und der lokalen Suche für Wiesbaden und Umgebung.</p>
        <ul class="svc-list">
          <li>Google Business Profil-Optimierung</li>
          <li>Lokale Citations &amp; Verzeichnisse</li>
          <li>Standortspezifische Landingpages</li>
        </ul>
      </article>

      <article class="svc-card fade-up" style="animation-delay: 0.2s">
        <span class="svc-num" aria-hidden="true">03</span>
        <div class="svc-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>
            <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>
          </svg>
        </div>
        <h3>Content &amp; SEO-Texte</h3>
        <p>Hochwertiger Content, der rankt und konvertiert. Unsere SEO-Texte sprechen Suchmaschinen und Ihre Kunden gleichzeitig an — verständlich, überzeugend, wirkungsvoll.</p>
        <ul class="svc-list">
          <li>SEO-optimierte Webseiten-Texte</li>
          <li>Blogartikel &amp; Ratgebercontent</li>
          <li>Landingpage-Optimierung</li>
        </ul>
      </article>

      <article class="svc-card fade-up" style="animation-delay: 0.3s">
        <span class="svc-num" aria-hidden="true">04</span>
        <div class="svc-icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <rect x="2" y="3" width="20" height="14" rx="2"/>
            <path d="M8 21h8M12 17v4"/>
            <path d="M7 8h.01M11 8h6"/>
            <path d="M7 12h.01M11 12h6"/>
          </svg>
        </div>
        <h3>Technisches SEO</h3>
        <p>Geschwindigkeit, Struktur, Crawlbarkeit — die technischen Grundlagen entscheiden, ob Google Ihre Seite wirklich versteht. Wir sorgen dafür, dass alles reibungslos läuft.</p>
        <ul class="svc-list">
          <li>Core Web Vitals Optimierung</li>
          <li>Strukturierte Daten &amp; Schema.org</li>
          <li>Crawling, Indexierung &amp; Ladezeit</li>
        </ul>
      </article>
    </div>
  </div>
</section>

<!-- PREISE -->
<section id="preise">
  <div class="section-wrap">
    <header class="section-header fade-up">
      <div>
        <p class="sec-tag"><span class="tag-line"></span>Transparente Preise</p>
        <h2 class="sec-title">Was kostet SEO<br/>in Wiesbaden?</h2>
      </div>
    </header>
    <div class="preise-grid">
      <div class="preis-card fade-up">
        <p class="preis-label">Starter</p>
        <div class="preis-price">ab 490 <span>€/Mo.</span></div>
        <p class="preis-desc">Für lokale Einzelunternehmer und kleine Betriebe, die in Wiesbaden sichtbarer werden wollen.</p>
        <ul class="preis-list">
          <li>Google Business Profil-Optimierung</li>
          <li>Lokale Keyword-Recherche</li>
          <li>Technischer Basis-Audit</li>
          <li>Monatliches Reporting</li>
        </ul>
        <a href="#kontakt" class="preis-cta">Anfragen →</a>
      </div>
      <div class="preis-card preis-featured fade-up" style="animation-delay:.1s">
        <span class="preis-badge">Beliebt</span>
        <p class="preis-label">Professional</p>
        <div class="preis-price">ab 890 <span>€/Mo.</span></div>
        <p class="preis-desc">Für wachsende Unternehmen, die langfristig auf Seite 1 ranken und organischen Traffic in Kunden umwandeln wollen.</p>
        <ul class="preis-list">
          <li>Alles aus Starter</li>
          <li>On-Page &amp; Content SEO</li>
          <li>Backlink-Aufbau</li>
          <li>Wettbewerbsanalyse</li>
          <li>Monatliche Strategie-Calls</li>
        </ul>
        <a href="#kontakt" class="preis-cta preis-cta-gold">Anfragen →</a>
      </div>
      <div class="preis-card fade-up" style="animation-delay:.2s">
        <p class="preis-label">Premium</p>
        <div class="preis-price">ab 1.490 <span>€/Mo.</span></div>
        <p class="preis-desc">Für Unternehmen, die vollumfängliche SEO-Betreuung und maximale Sichtbarkeit im Rhein-Main-Gebiet anstreben.</p>
        <ul class="preis-list">
          <li>Alles aus Professional</li>
          <li>Technisches SEO &amp; Core Web Vitals</li>
          <li>Content-Produktion</li>
          <li>Dedizierter Ansprechpartner</li>
          <li>Quartals-Strategie &amp; Roadmap</li>
        </ul>
        <a href="#kontakt" class="preis-cta">Anfragen →</a>
      </div>
    </div>
    <p class="preise-hint fade-up">Keine Mindestvertragslaufzeit. Individuelle Pakete auf Anfrage.</p>
  </div>
</section>

<!-- WARUM WIR -->
<section id="warum-wir">
  <div class="section-wrap warum-wrap">
    <div class="warum-content fade-up">
      <span class="sec-num-block" aria-hidden="true">02</span>
      <p class="sec-tag"><span class="tag-line"></span>Warum SEO Wiesbaden</p>
      <h2 class="sec-title">Lokal verwurzelt.<br/>Digital exzellent.</h2>
      <p class="warum-lead">Ich bin Lukasz — SEO-Berater aus Wiesbaden. Seit über 8 Jahren helfe ich lokalen Unternehmen im Rhein-Main-Gebiet dabei, bei Google gefunden zu werden. Persönlich, transparent und mit messbaren Ergebnissen.</p>
      <ul class="warum-list">
        <li>
          <div class="check-icon" aria-hidden="true">✓</div>
          <div>
            <strong>Transparente Berichterstattung</strong>
            <p>Monatliche Reports mit echten Kennzahlen — kein Marketing-Sprech, nur Fakten.</p>
          </div>
        </li>
        <li>
          <div class="check-icon" aria-hidden="true">✓</div>
          <div>
            <strong>Persönlicher Ansprechpartner</strong>
            <p>Kein Ticketsystem. Sie haben immer denselben Ansprechpartner, der Ihr Business kennt.</p>
          </div>
        </li>
        <li>
          <div class="check-icon" aria-hidden="true">✓</div>
          <div>
            <strong>Messbare Ergebnisse</strong>
            <p>Klare Ziele, nachweisbare Verbesserungen. Wir arbeiten ergebnisorientiert — nicht nach Stunden.</p>
          </div>
        </li>
        <li>
          <div class="check-icon" aria-hidden="true">✓</div>
          <div>
            <strong>Keine Knebelverträge</strong>
            <p>Faire Laufzeiten, flexible Zusammenarbeit. Wir überzeugen durch Leistung, nicht durch Paragraphen.</p>
          </div>
        </li>
      </ul>
    </div>

    <div class="warum-visual fade-up" aria-hidden="true">
      <div class="vis-card vis-big">
        <span class="vis-label">Organischer Traffic</span>
        <span class="vis-value">+340%</span>
        <div class="vis-bars">
          <div class="vbar" style="height:18%"></div>
          <div class="vbar" style="height:28%"></div>
          <div class="vbar" style="height:38%"></div>
          <div class="vbar" style="height:50%"></div>
          <div class="vbar" style="height:62%"></div>
          <div class="vbar" style="height:58%"></div>
          <div class="vbar" style="height:74%"></div>
          <div class="vbar" style="height:88%"></div>
          <div class="vbar vbar-gold" style="height:100%"></div>
        </div>
        <span class="vis-period">12-Monats-Verlauf</span>
      </div>
      <div class="vis-card vis-small">
        <span class="vis-label">Google Position</span>
        <span class="vis-big-num">#1</span>
        <span class="vis-kw">"Anwalt Wiesbaden"</span>
      </div>
      <div class="vis-card vis-small">
        <span class="vis-label">Top-3 Rankings</span>
        <span class="vis-big-num">+47</span>
        <span class="vis-period">in 6 Monaten</span>
      </div>
    </div>
  </div>
</section>

<!-- PROZESS -->
<section id="prozess">
  <div class="section-wrap">
    <header class="section-header fade-up">
      <span class="sec-num" aria-hidden="true">03</span>
      <div>
        <p class="sec-tag"><span class="tag-line"></span>Wie wir arbeiten</p>
        <h2 class="sec-title">Von der Analyse<br/>bis zum Ranking</h2>
      </div>
    </header>

    <div class="prozess-grid">
      <div class="pstep fade-up">
        <div class="pstep-num">01</div>
        <h3>Analyse</h3>
        <p>Wir analysieren Ihre aktuelle Sichtbarkeit, Ihre Wettbewerber und das Suchverhalten Ihrer Zielgruppe in Wiesbaden. Ergebnis: ein klares Bild Ihrer Chancen.</p>
      </div>
      <div class="pstep fade-up" style="animation-delay:0.1s">
        <div class="pstep-num">02</div>
        <h3>Strategie</h3>
        <p>Auf Basis der Analyse entwickeln wir einen maßgeschneiderten SEO-Plan — priorisiert nach Potenzial und Aufwand, abgestimmt auf Ihr Budget und Ihre Ziele.</p>
      </div>
      <div class="pstep fade-up" style="animation-delay:0.2s">
        <div class="pstep-num">03</div>
        <h3>Umsetzung</h3>
        <p>Wir implementieren die Optimierungen — technisch, inhaltlich und strukturell. Transparent, mit klaren Meilensteinen und regelmäßigen Updates für Sie.</p>
      </div>
      <div class="pstep fade-up" style="animation-delay:0.3s">
        <div class="pstep-num">04</div>
        <h3>Optimierung</h3>
        <p>SEO ist kein Sprint, sondern ein Marathon. Wir beobachten, messen und optimieren kontinuierlich — damit Ihr Wachstum nachhaltig und stabil bleibt.</p>
      </div>
    </div>
  </div>
</section>

<!-- ERGEBNISSE -->
<section id="ergebnisse">
  <div class="section-wrap">
    <header class="section-header fade-up">
      <span class="sec-num" aria-hidden="true">04</span>
      <div>
        <p class="sec-tag"><span class="tag-line"></span>Unsere Arbeit</p>
        <h2 class="sec-title">Ergebnisse,<br/>die überzeugen</h2>
      </div>
    </header>

    <div class="results-grid">
      <article class="result-card fade-up">
        <p class="res-cat">Rechtsberatung · Wiesbaden</p>
        <h3>Anwaltskanzlei steigert Mandantenanfragen um 280%</h3>
        <p>Durch Local-SEO-Optimierung und strukturierten Contentaufbau kletterte die Kanzlei für über 20 relevante Keywords auf die erste Google-Seite — dauerhaft.</p>
        <div class="res-metrics">
          <div class="res-metric">
            <span class="mval">+340%</span>
            <span class="mlabel">organischer Traffic</span>
          </div>
          <div class="res-metric">
            <span class="mval">#1</span>
            <span class="mlabel">für "Anwalt Wiesbaden"</span>
          </div>
          <div class="res-metric">
            <span class="mval">8 Mo.</span>
            <span class="mlabel">bis zum Ergebnis</span>
          </div>
        </div>
      </article>

      <article class="result-card fade-up" style="animation-delay:0.1s">
        <p class="res-cat">Gesundheit · Wiesbaden / Mainz</p>
        <h3>Zahnarztpraxis dominiert lokale Suchen im Rhein-Main-Gebiet</h3>
        <p>Mit einer umfassenden Local-SEO-Kampagne und Google-Business-Optimierung belegt die Praxis heute Positionen 1–3 für alle relevanten Suchbegriffe.</p>
        <div class="res-metrics">
          <div class="res-metric">
            <span class="mval">12</span>
            <span class="mlabel">Seite-1-Keywords</span>
          </div>
          <div class="res-metric">
            <span class="mval">+190%</span>
            <span class="mlabel">Neupatienten online</span>
          </div>
          <div class="res-metric">
            <span class="mval">5 Mo.</span>
            <span class="mlabel">bis zum Ergebnis</span>
          </div>
        </div>
      </article>

      <article class="result-card fade-up" style="animation-delay:0.2s">
        <p class="res-cat">Immobilien · Wiesbaden</p>
        <h3>Immobilienmakler verdreifacht qualifizierte Online-Anfragen</h3>
        <p>Contentmarketing, technische Optimierungen und lokale Verlinkungsstrategie steigerten den organischen Traffic mit direktem Einfluss auf Abschlüsse und Umsatz.</p>
        <div class="res-metrics">
          <div class="res-metric">
            <span class="mval">+180%</span>
            <span class="mlabel">qualifizierte Anfragen</span>
          </div>
          <div class="res-metric">
            <span class="mval">×3</span>
            <span class="mlabel">organischer Traffic</span>
          </div>
          <div class="res-metric">
            <span class="mval">10 Mo.</span>
            <span class="mlabel">bis zum Ergebnis</span>
          </div>
        </div>
      </article>
    </div>
  </div>
</section>

<!-- STIMMEN -->
<section id="stimmen">
  <div class="section-wrap">
    <header class="section-header fade-up">
      <div>
        <p class="sec-tag"><span class="tag-line"></span>Kundenstimmen</p>
        <h2 class="sec-title">Was Kunden über<br/>SEO Wiesbaden sagen</h2>
      </div>
    </header>
    <div class="stimmen-grid">
      <blockquote class="stimme-card fade-up">
        <div class="stimme-quote" aria-hidden="true">"</div>
        <p>Suchmaschinenmarketing war bei uns ein voller Erfolg. Danke an Lukasz und das Team. Das habt ihr echt gut gemacht!</p>
        <footer>
          <strong>Dani</strong>
          <span>Kunde, Wiesbaden</span>
        </footer>
      </blockquote>
    </div>
  </div>
</section>

<!-- FAQ -->
<section id="faq">
  <div class="section-wrap">
    <header class="section-header fade-up">
      <div>
        <p class="sec-tag"><span class="tag-line"></span>Häufige Fragen</p>
        <h2 class="sec-title">Ihre Fragen zu<br/>SEO Wiesbaden</h2>
      </div>
    </header>
    <div class="faq-list" itemscope itemtype="https://schema.org/FAQPage">
      <details class="faq-item fade-up" itemscope itemprop="mainEntity" itemtype="https://schema.org/Question">
        <summary itemprop="name">Wie lange dauert es, bis SEO Ergebnisse sichtbar werden?</summary>
        <div class="faq-answer" itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
          <p itemprop="text">Erste messbare Verbesserungen sind oft nach 3–4 Monaten sichtbar. Nachhaltige Top-Rankings entstehen in der Regel nach 6–12 Monaten. SEO ist eine langfristige Investition — die Ergebnisse bleiben dafür dauerhaft und wachsen weiter.</p>
        </div>
      </details>
      <details class="faq-item fade-up" itemscope itemprop="mainEntity" itemtype="https://schema.org/Question">
        <summary itemprop="name">Was kostet SEO in Wiesbaden?</summary>
        <div class="faq-answer" itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
          <p itemprop="text">Unsere Pakete starten ab 490 €/Monat für lokale SEO-Maßnahmen und gehen bis 1.490 €/Monat für Full-Service-Betreuung. Der genaue Preis hängt von Ihrer Ausgangssituation, dem Wettbewerb und Ihren Zielen ab — gerne erstellen wir ein individuelles Angebot.</p>
        </div>
      </details>
      <details class="faq-item fade-up" itemscope itemprop="mainEntity" itemtype="https://schema.org/Question">
        <summary itemprop="name">Brauche ich SEO, wenn ich bereits Google Ads schalte?</summary>
        <div class="faq-answer" itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
          <p itemprop="text">Ja — Google Ads und SEO ergänzen sich ideal. Ads liefern sofortige Sichtbarkeit, kosten aber bei jedem Klick. SEO baut organische Rankings auf, die dauerhaft Traffic bringen — ohne laufende Klickkosten. Langfristig ist SEO deutlich kosteneffizienter.</p>
        </div>
      </details>
      <details class="faq-item fade-up" itemscope itemprop="mainEntity" itemtype="https://schema.org/Question">
        <summary itemprop="name">Wie messen Sie den SEO-Erfolg?</summary>
        <div class="faq-answer" itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
          <p itemprop="text">Wir tracken organischen Traffic, Keyword-Rankings, Sichtbarkeitsindex und Conversion-Raten. Monatlich erhalten Sie einen klaren Report mit allen relevanten Kennzahlen — verständlich erklärt, ohne Marketing-Sprech.</p>
        </div>
      </details>
      <details class="faq-item fade-up" itemscope itemprop="mainEntity" itemtype="https://schema.org/Question">
        <summary itemprop="name">Arbeiten Sie nur für Unternehmen in Wiesbaden?</summary>
        <div class="faq-answer" itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
          <p itemprop="text">Der Schwerpunkt liegt auf Wiesbaden und dem Rhein-Main-Gebiet — Mainz, Frankfurt, Darmstadt und Umgebung. Lokale Marktkenntnis ist unser klarer Vorteil. Für ausgewählte nationale Projekte sind wir ebenfalls offen.</p>
        </div>
      </details>
    </div>
  </div>
</section>

<!-- BLOG VORSCHAU -->
<section id="blog-vorschau">
  <div class="section-wrap">
    <header class="section-header fade-up">
      <div>
        <p class="sec-tag"><span class="tag-line"></span>Aus unserem Blog</p>
        <h2 class="sec-title">SEO-Wissen für<br/>lokale Unternehmen</h2>
      </div>
    </header>
    <div class="blog-grid">
      {recentPosts.map((post: any) => (
        <a href={`/blog/${post.frontmatter.slug}`} class="blog-card fade-up">
          <span class="blog-date">{new Date(post.frontmatter.pubDate).toLocaleDateString('de-DE', { day: '2-digit', month: 'long', year: 'numeric' })}</span>
          <h3>{post.frontmatter.title}</h3>
          <p>{post.frontmatter.description}</p>
          <span class="blog-more">Weiterlesen →</span>
        </a>
      ))}
    </div>
    <div class="blog-all fade-up">
      <a href="/blog" class="btn-ghost">Alle Artikel ansehen →</a>
    </div>
  </div>
</section>

<!-- KONTAKT -->
<section id="kontakt">
  <div class="kontakt-wrap">
    <div class="kontakt-glow" aria-hidden="true"></div>
    <span class="sec-num-center" aria-hidden="true">05</span>
    <p class="sec-tag sec-tag-center"><span class="tag-line"></span>Jetzt starten</p>
    <h2 class="kontakt-title">Bereit, in Wiesbaden<br/>gefunden zu werden?</h2>
    <p class="kontakt-sub">Wir analysieren kostenlos Ihre aktuelle SEO-Situation und zeigen Ihnen, welches Potenzial in Ihrer Website steckt.</p>

    <form class="kform" name="kontakt" method="POST" data-netlify="true">
      <input type="hidden" name="form-name" value="kontakt" />
      <div class="form-row">
        <div class="fg">
          <label for="name">Name</label>
          <input type="text" id="name" name="name" placeholder="Max Mustermann" required />
        </div>
        <div class="fg">
          <label for="email">E-Mail</label>
          <input type="email" id="email" name="email" placeholder="max@unternehmen.de" required />
        </div>
      </div>
      <div class="fg">
        <label for="website">Website</label>
        <input type="url" id="website" name="website" placeholder="https://ihr-unternehmen.de" />
      </div>
      <div class="fg">
        <label for="nachricht">Was möchten Sie erreichen?</label>
        <textarea id="nachricht" name="nachricht" rows="4" placeholder="Kurze Beschreibung Ihrer Situation und Ziele..."></textarea>
      </div>
      <label class="privacy-check">
        <input type="checkbox" name="datenschutz" required />
        <span>Ich habe die <a href="/datenschutz">Datenschutzhinweise</a> gelesen und bin mit der Verarbeitung meiner Angaben zur Beantwortung meiner Anfrage einverstanden.</span>
      </label>
      <button type="submit" class="btn-submit">Kostenlose Analyse anfragen →</button>
    </form>
  </div>
</section>

<!-- FOOTER -->
<footer id="footer">
  <div class="footer-wrap">
    <div class="footer-brand">
      <a href="/" class="logo" aria-label="SEO Wiesbaden">
        <span class="logo-rank">SEO </span><span class="logo-rhein">Wiesbaden</span>
      </a>
      <p>SEO Agentur Wiesbaden<br/>Suchmaschinenoptimierung für den Rhein-Main-Raum</p>
    </div>
    <nav class="footer-links" aria-label="Footer Navigation">
      <div>
        <strong>Leistungen</strong>
        <ul>
          <li><a href="#leistungen">SEO-Strategie</a></li>
          <li><a href="#leistungen">Local SEO</a></li>
          <li><a href="#leistungen">Content SEO</a></li>
          <li><a href="#leistungen">Technisches SEO</a></li>
        </ul>
      </div>
      <div>
        <strong>Unternehmen</strong>
        <ul>
          <li><a href="#warum-wir">Über uns</a></li>
          <li><a href="#ergebnisse">Ergebnisse</a></li>
          <li><a href="/blog">Blog</a></li>
          <li><a href="#kontakt">Kontakt</a></li>
        </ul>
      </div>
    </nav>
  </div>
  <div class="footer-bottom">
    <p>© 2026 SEO Wiesbaden · <a href="/impressum">Impressum</a> · <a href="/datenschutz">Datenschutz</a></p>
  </div>
</footer>

<script>
  // Nav scroll effect
  const nav = document.getElementById('nav');
  window.addEventListener('scroll', () => {
    nav?.classList.toggle('scrolled', window.scrollY > 60);
  }, { passive: true });

  // Mobile burger
  const burger = document.getElementById('burger');
  const mobileMenu = document.getElementById('mobileMenu');
  burger?.addEventListener('click', () => {
    const open = mobileMenu?.classList.toggle('open');
    burger.setAttribute('aria-expanded', String(open));
  });
  mobileMenu?.querySelectorAll('a').forEach(a => {
    a.addEventListener('click', () => mobileMenu.classList.remove('open'));
  });

  // Scroll-triggered fade-up animations
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.12 });
  document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));
</script>

<style>
  :root {
    --bg:       #0C1527;
    --bg-mid:   #101D34;
    --bg-light: #152040;
    --gold:     #C8A96E;
    --gold-lt:  #E8D5A8;
    --gold-dk:  #9A7B45;
    --cream:    #F5F0E8;
    --txt:      #B8B0A4;
    --txt-dim:  #5A6278;
    --txt-hi:   #EDE8DF;
    --bdr:      rgba(200,169,110,0.12);
    --bdr-hi:   rgba(200,169,110,0.28);
    --serif:    Georgia, 'Times New Roman', serif;
    --sans:     Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--txt);
    font-family: var(--sans);
    font-size: 16px;
    line-height: 1.65;
    overflow-x: hidden;
  }

  ::-webkit-scrollbar { width: 5px; }
  ::-webkit-scrollbar-track { background: var(--bg); }
  ::-webkit-scrollbar-thumb { background: var(--gold-dk); border-radius: 3px; }

  h1, h2, h3 {
    font-family: var(--serif);
    color: var(--txt-hi);
    font-weight: 600;
    line-height: 1.12;
  }

  a { color: inherit; text-decoration: none; }
  ul { list-style: none; }
  address { font-style: normal; }

  /* ── NAV ── */
  #nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    padding: 1.4rem 0;
    transition: background .3s, padding .3s, border-color .3s;
    border-bottom: 1px solid transparent;
  }
  #nav.scrolled {
    background: rgba(12,21,39,.94);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    padding: .85rem 0;
    border-color: var(--bdr);
  }
  .nav-inner {
    max-width: 1200px; margin: 0 auto; padding: 0 2rem;
    display: flex; align-items: center; gap: 2rem;
  }
  .logo { font-family: var(--serif); font-size: 1.55rem; font-weight: 700; }
  .logo-rank { color: var(--txt-hi); }
  .logo-rhein { color: var(--gold); }
  .nav-links { display: flex; gap: 2rem; margin-left: auto; }
  .nav-links a {
    font-size: .8rem; font-weight: 500; letter-spacing: .08em;
    text-transform: uppercase; color: var(--txt);
    transition: color .2s;
  }
  .nav-links a:hover { color: var(--gold); }
  .nav-cta {
    font-size: .75rem; font-weight: 700; letter-spacing: .08em;
    text-transform: uppercase; color: var(--bg); background: var(--gold);
    padding: .6rem 1.4rem; border-radius: 2px;
    transition: background .2s, transform .15s; white-space: nowrap;
  }
  .nav-cta:hover { background: var(--gold-lt); transform: translateY(-1px); }
  .nav-burger {
    display: none; flex-direction: column; gap: 5px;
    background: none; border: none; cursor: pointer; padding: 4px; margin-left: auto;
  }
  .nav-burger span { display: block; width: 24px; height: 1.5px; background: var(--gold); transition: .3s; }
  .mobile-menu {
    display: none; flex-direction: column;
    background: rgba(12,21,39,.97); border-top: 1px solid var(--bdr);
    padding: 1.5rem 2rem 2rem;
  }
  .mobile-menu.open { display: flex; }
  .mobile-menu a {
    font-size: .9rem; font-weight: 500; padding: .75rem 0;
    border-bottom: 1px solid var(--bdr); color: var(--txt);
    transition: color .2s;
  }
  .mobile-menu a:hover { color: var(--gold); }
  .mobile-cta {
    margin-top: 1rem; text-align: center; border: 1px solid var(--gold) !important;
    color: var(--gold) !important; border-radius: 2px; font-weight: 700 !important;
  }

  /* ── HERO ── */
  #hero {
    position: relative; min-height: 100svh;
    display: flex; align-items: center;
    overflow: hidden; padding: 8rem 2rem 5rem;
  }
  .hero-bg-word {
    position: absolute; top: 50%; right: -4%;
    transform: translateY(-50%);
    font-family: var(--serif);
    font-size: clamp(180px, 28vw, 400px);
    font-weight: 700; line-height: 1;
    color: transparent;
    -webkit-text-stroke: 1px rgba(200,169,110,.055);
    pointer-events: none; user-select: none;
    letter-spacing: -.02em;
  }
  .hero-grid {
    position: absolute; inset: 0;
    background-image:
      linear-gradient(rgba(200,169,110,.035) 1px, transparent 1px),
      linear-gradient(90deg, rgba(200,169,110,.035) 1px, transparent 1px);
    background-size: 64px 64px;
    pointer-events: none;
  }
  .hero-inner { position: relative; max-width: 1200px; margin: 0 auto; width: 100%; }
  .hero-tag {
    display: flex; align-items: center; gap: 1rem;
    font-size: .7rem; font-weight: 600; letter-spacing: .2em;
    text-transform: uppercase; color: var(--gold);
    margin-bottom: 2rem;
  }
  .tag-line { display: block; width: 36px; height: 1px; background: var(--gold); flex-shrink: 0; }
  .hero-headline {
    font-family: var(--serif);
    font-size: clamp(4rem, 9.5vw, 9rem);
    font-weight: 600; letter-spacing: -.025em; line-height: .95;
    color: var(--txt-hi); margin-bottom: 2.5rem;
  }
  .hl-1, .hl-3 { display: block; }
  .hl-2 { display: block; color: var(--gold); font-style: italic; }
  .hero-sub {
    font-size: 1.1rem; line-height: 1.72;
    color: var(--txt); max-width: 520px; margin-bottom: 3rem;
  }
  .hero-actions { display: flex; gap: 1.25rem; flex-wrap: wrap; align-items: center; }
  .btn-primary {
    display: inline-flex; align-items: center;
    background: var(--gold); color: var(--bg);
    font-size: .82rem; font-weight: 700; letter-spacing: .07em;
    text-transform: uppercase; padding: 1rem 2.2rem; border-radius: 2px;
    transition: background .2s, transform .15s, box-shadow .2s;
  }
  .btn-primary:hover {
    background: var(--gold-lt); transform: translateY(-2px);
    box-shadow: 0 10px 32px rgba(200,169,110,.22);
  }
  .btn-ghost {
    display: inline-flex; align-items: center;
    color: var(--txt); font-size: .9rem; font-weight: 500;
    padding-bottom: 3px; border-bottom: 1px solid var(--bdr-hi);
    transition: color .2s, border-color .2s;
  }
  .btn-ghost:hover { color: var(--gold); border-color: var(--gold); }
  .hero-scroll {
    position: absolute; bottom: 2.5rem; left: 50%;
    transform: translateX(-50%);
    display: flex; flex-direction: column; align-items: center; gap: .75rem;
    font-size: .6rem; letter-spacing: .2em; text-transform: uppercase;
    color: var(--txt-dim);
  }
  .scroll-line {
    width: 1px; height: 48px;
    background: linear-gradient(to bottom, var(--gold-dk), transparent);
    animation: scrollPulse 1.6s ease-in-out infinite;
  }
  @keyframes scrollPulse {
    0%   { transform: scaleY(0); transform-origin: top; opacity: 1; }
    49%  { transform: scaleY(1); transform-origin: top; opacity: 1; }
    50%  { transform: scaleY(1); transform-origin: bottom; opacity: 1; }
    100% { transform: scaleY(0); transform-origin: bottom; opacity: 0; }
  }

  /* ── STATS ── */
  #stats {
    background: var(--bg-mid);
    border-top: 1px solid var(--bdr);
    border-bottom: 1px solid var(--bdr);
  }
  .stats-inner {
    max-width: 1200px; margin: 0 auto; padding: 3rem 2rem;
    display: flex; justify-content: center; flex-wrap: wrap;
  }
  .stat {
    display: flex; flex-direction: column; align-items: center;
    gap: .35rem; padding: 1.5rem 3.5rem; text-align: center;
  }
  .stat-div { width: 1px; background: var(--bdr-hi); margin: 1rem 0; align-self: stretch; }
  .stat-n {
    font-family: var(--serif); font-size: 3.5rem;
    font-weight: 600; color: var(--txt-hi); line-height: 1;
  }
  .stat-n sup { font-size: 1.8rem; color: var(--gold); vertical-align: super; }
  .stat-l { font-size: .7rem; letter-spacing: .12em; text-transform: uppercase; color: var(--txt-dim); }

  /* ── SECTION COMMONS ── */
  .section-wrap { max-width: 1200px; margin: 0 auto; padding: 7rem 2rem; }
  .section-header { display: flex; align-items: flex-start; gap: 2.5rem; margin-bottom: 4.5rem; }
  .sec-num {
    font-family: var(--serif); font-size: 5.5rem; font-weight: 700;
    color: transparent; -webkit-text-stroke: 1px rgba(200,169,110,.18);
    line-height: 1; flex-shrink: 0; margin-top: -.4rem;
  }
  .sec-num-block {
    display: block; font-family: var(--serif); font-size: 4rem; font-weight: 700;
    color: transparent; -webkit-text-stroke: 1px rgba(200,169,110,.18);
    line-height: 1; margin-bottom: .5rem;
  }
  .sec-num-center {
    display: block; font-family: var(--serif); font-size: 4rem; font-weight: 700;
    color: transparent; -webkit-text-stroke: 1px rgba(200,169,110,.18);
    line-height: 1; text-align: center; margin-bottom: .75rem;
  }
  .sec-tag {
    display: flex; align-items: center; gap: .75rem;
    font-size: .68rem; font-weight: 600; letter-spacing: .2em;
    text-transform: uppercase; color: var(--gold); margin-bottom: .9rem;
  }
  .sec-tag-center { justify-content: center; }
  .sec-title { font-family: var(--serif); font-size: clamp(2rem, 4vw, 3.25rem); font-weight: 600; }

  /* ── LEISTUNGEN ── */
  #leistungen { border-top: 1px solid var(--bdr); }
  .services-grid {
    display: grid; grid-template-columns: repeat(2, 1fr);
    border: 1px solid var(--bdr); gap: 1px; background: var(--bdr);
  }
  .svc-card {
    background: var(--bg); padding: 2.5rem;
    position: relative; overflow: hidden;
    transition: background .3s;
  }
  .svc-card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px;
    background: var(--gold); transform: scaleX(0); transform-origin: left;
    transition: transform .4s ease;
  }
  .svc-card:hover { background: var(--bg-mid); }
  .svc-card:hover::before { transform: scaleX(1); }
  .svc-num {
    position: absolute; top: 1.5rem; right: 2rem;
    font-family: var(--serif); font-size: 5rem; font-weight: 700;
    color: transparent; -webkit-text-stroke: 1px rgba(200,169,110,.07);
    line-height: 1; pointer-events: none;
  }
  .svc-icon { width: 42px; height: 42px; color: var(--gold); margin-bottom: 1.5rem; }
  .svc-icon svg { width: 100%; height: 100%; }
  .svc-card h3 { font-size: 1.55rem; font-weight: 600; margin-bottom: 1rem; }
  .svc-card p { font-size: .88rem; line-height: 1.72; color: var(--txt); margin-bottom: 1.5rem; }
  .svc-list { display: flex; flex-direction: column; gap: .4rem; }
  .svc-list li {
    font-size: .78rem; color: var(--txt-dim);
    display: flex; align-items: center; gap: .6rem; letter-spacing: .02em;
  }
  .svc-list li::before { content: '—'; color: var(--gold); font-size: .7rem; flex-shrink: 0; }

  /* ── WARUM WIR ── */
  #warum-wir {
    background: var(--bg-mid);
    border-top: 1px solid var(--bdr); border-bottom: 1px solid var(--bdr);
  }
  .warum-wrap { display: grid; grid-template-columns: 1fr 1fr; gap: 6rem; align-items: center; }
  .warum-lead { font-size: 1rem; line-height: 1.72; color: var(--txt); margin-bottom: 2.5rem; }
  .warum-list { display: flex; flex-direction: column; gap: 1.5rem; }
  .warum-list li { display: flex; gap: 1rem; align-items: flex-start; }
  .check-icon {
    width: 26px; height: 26px; border-radius: 50%;
    background: rgba(200,169,110,.08); border: 1px solid var(--bdr-hi);
    display: flex; align-items: center; justify-content: center;
    color: var(--gold); font-size: .72rem; flex-shrink: 0; margin-top: .1rem;
  }
  .warum-list strong {
    display: block; font-size: .94rem; font-weight: 600;
    color: var(--txt-hi); margin-bottom: .2rem; font-family: var(--sans);
  }
  .warum-list p { font-size: .86rem; color: var(--txt); line-height: 1.62; }

  .warum-visual { display: grid; grid-template-columns: 1fr 1fr; grid-template-rows: auto auto; gap: 1rem; }
  .vis-card {
    background: var(--bg); border: 1px solid var(--bdr);
    border-radius: 6px; padding: 1.6rem;
  }
  .vis-big { grid-column: 1 / -1; }
  .vis-label {
    display: block; font-size: .65rem; letter-spacing: .14em;
    text-transform: uppercase; color: var(--txt-dim); margin-bottom: .5rem;
  }
  .vis-value {
    display: block; font-family: var(--serif); font-size: 2.6rem;
    font-weight: 700; color: var(--gold); line-height: 1; margin-bottom: 1.25rem;
  }
  .vis-bars { display: flex; align-items: flex-end; gap: 5px; height: 56px; }
  .vbar { flex: 1; background: rgba(200,169,110,.18); border-radius: 2px; }
  .vbar-gold { background: var(--gold); }
  .vis-period { display: block; font-size: .65rem; color: var(--txt-dim); margin-top: .75rem; letter-spacing: .1em; }
  .vis-big-num {
    display: block; font-family: var(--serif); font-size: 3rem;
    font-weight: 700; color: var(--gold); line-height: 1; margin-bottom: .3rem;
  }
  .vis-kw { font-size: .72rem; color: var(--txt-dim); font-style: italic; }

  /* ── PROZESS ── */
  #prozess { border-top: 1px solid var(--bdr); }
  .prozess-grid {
    display: grid; grid-template-columns: repeat(4, 1fr);
    border: 1px solid var(--bdr); border-right: none;
  }
  .pstep {
    padding: 2.5rem; border-right: 1px solid var(--bdr);
    transition: background .3s;
  }
  .pstep:hover { background: var(--bg-mid); }
  .pstep-num {
    font-family: var(--serif); font-size: 3.5rem; font-weight: 700;
    color: var(--gold); opacity: .45; line-height: 1; margin-bottom: 1.5rem;
  }
  .pstep h3 { font-size: 1.4rem; font-weight: 600; margin-bottom: .75rem; }
  .pstep p { font-size: .86rem; line-height: 1.7; color: var(--txt); }

  /* ── ERGEBNISSE ── */
  #ergebnisse {
    background: var(--bg-mid);
    border-top: 1px solid var(--bdr); border-bottom: 1px solid var(--bdr);
  }
  .results-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; }
  .result-card {
    background: var(--bg); border: 1px solid var(--bdr);
    border-radius: 4px; padding: 2rem;
    position: relative; overflow: hidden;
    transition: border-color .3s, transform .25s;
  }
  .result-card::after {
    content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 2px;
    background: linear-gradient(90deg, var(--gold), transparent);
  }
  .result-card:hover { border-color: var(--bdr-hi); transform: translateY(-4px); }
  .res-cat {
    font-size: .67rem; letter-spacing: .14em; text-transform: uppercase;
    color: var(--gold); margin-bottom: 1rem;
  }
  .result-card h3 { font-size: 1.3rem; font-weight: 600; margin-bottom: 1rem; line-height: 1.25; }
  .result-card p { font-size: .86rem; line-height: 1.7; color: var(--txt); margin-bottom: 1.75rem; }
  .res-metrics {
    display: flex; gap: 1rem; padding-top: 1.25rem;
    border-top: 1px solid var(--bdr);
  }
  .res-metric { flex: 1; text-align: center; }
  .mval {
    display: block; font-family: var(--serif); font-size: 1.65rem;
    font-weight: 700; color: var(--gold); line-height: 1; margin-bottom: .25rem;
  }
  .mlabel { display: block; font-size: .6rem; letter-spacing: .08em; color: var(--txt-dim); text-transform: uppercase; }

  /* ── KONTAKT ── */
  #kontakt { border-top: 1px solid var(--bdr); position: relative; overflow: hidden; }
  .kontakt-glow {
    position: absolute; top: -200px; left: 50%; transform: translateX(-50%);
    width: 700px; height: 700px;
    background: radial-gradient(circle, rgba(200,169,110,.04) 0%, transparent 70%);
    pointer-events: none;
  }
  .kontakt-wrap {
    max-width: 720px; margin: 0 auto;
    padding: 7rem 2rem; text-align: center; position: relative;
  }
  .kontakt-title {
    font-family: var(--serif);
    font-size: clamp(2.25rem, 5.5vw, 4rem);
    font-weight: 600; margin-bottom: 1.25rem; line-height: 1.08;
  }
  .kontakt-sub {
    font-size: 1rem; color: var(--txt); line-height: 1.72;
    margin-bottom: 3rem; max-width: 480px; margin-left: auto; margin-right: auto;
  }
  .kform { text-align: left; }
  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin-bottom: 1rem; }
  .fg { display: flex; flex-direction: column; gap: .45rem; margin-bottom: 1rem; }
  .fg:last-of-type { margin-bottom: 0; }
  .fg label {
    font-size: .68rem; letter-spacing: .12em; text-transform: uppercase;
    color: var(--txt-dim); font-weight: 500;
  }
  .fg input, .fg textarea {
    background: var(--bg-mid); border: 1px solid var(--bdr);
    border-radius: 3px; padding: .9rem 1rem;
    color: var(--txt-hi); font-family: var(--sans); font-size: .9rem;
    outline: none; transition: border-color .2s; width: 100%;
  }
  .fg input::placeholder, .fg textarea::placeholder { color: var(--txt-dim); }
  .fg input:focus, .fg textarea:focus { border-color: var(--gold); }
  .fg textarea { resize: vertical; min-height: 110px; }
  .privacy-check {
    display: flex; align-items: flex-start; gap: .7rem;
    margin-top: 1.25rem; font-size: .78rem; line-height: 1.55;
    color: var(--txt-dim);
  }
  .privacy-check input {
    width: 16px; height: 16px; flex: 0 0 16px; margin-top: .22rem;
    accent-color: var(--gold);
  }
  .privacy-check a { color: var(--gold); border-bottom: 1px solid var(--bdr-hi); }
  .privacy-check a:hover { border-color: var(--gold); }
  .btn-submit {
    margin-top: 1.75rem; width: 100%;
    background: var(--gold); color: var(--bg);
    font-family: var(--sans); font-size: .88rem; font-weight: 700;
    letter-spacing: .07em; text-transform: uppercase;
    padding: 1.1rem 2rem; border-radius: 2px; border: none; cursor: pointer;
    transition: background .2s, transform .15s, box-shadow .2s;
  }
  .btn-submit:hover {
    background: var(--gold-lt); transform: translateY(-2px);
    box-shadow: 0 10px 32px rgba(200,169,110,.2);
  }

  /* ── FOOTER ── */
  #footer { background: var(--bg); border-top: 1px solid var(--bdr); }
  .footer-wrap {
    max-width: 1200px; margin: 0 auto; padding: 4rem 2rem 3rem;
    display: grid; grid-template-columns: 2fr 2fr; gap: 4rem;
  }
  .footer-brand .logo { font-size: 1.65rem; display: inline-block; margin-bottom: 1rem; }
  .footer-brand p { font-size: .84rem; color: var(--txt-dim); line-height: 1.65; }
  .footer-links { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; }
  .footer-links strong {
    display: block; font-size: .65rem; letter-spacing: .16em;
    text-transform: uppercase; color: var(--txt-dim); margin-bottom: 1.2rem; font-weight: 600;
  }
  .footer-links ul { display: flex; flex-direction: column; gap: .55rem; }
  .footer-links a { font-size: .86rem; color: var(--txt); transition: color .2s; }
  .footer-links a:hover { color: var(--gold); }
  .footer-bottom {
    border-top: 1px solid var(--bdr);
    padding: 1.5rem 2rem;
    max-width: 1200px; margin: 0 auto;
  }
  .footer-bottom p { font-size: .72rem; color: var(--txt-dim); }
  .footer-bottom a { color: var(--txt-dim); transition: color .2s; }
  .footer-bottom a:hover { color: var(--gold); }

  /* ── PREISE ── */
  #preise { border-top: 1px solid var(--bdr); background: var(--bg-mid); }
  .preise-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; }
  .preis-card {
    background: var(--bg); border: 1px solid var(--bdr); border-radius: 4px;
    padding: 2.25rem 2rem; display: flex; flex-direction: column; gap: 0; position: relative;
    transition: border-color .3s, transform .25s;
  }
  .preis-card:hover { border-color: var(--bdr-hi); transform: translateY(-3px); }
  .preis-featured {
    background: var(--bg-light); border-color: var(--gold);
    box-shadow: 0 0 40px rgba(200,169,110,.08);
  }
  .preis-badge {
    position: absolute; top: -1px; right: 1.5rem;
    background: var(--gold); color: var(--bg);
    font-size: .62rem; font-weight: 800; letter-spacing: .12em;
    text-transform: uppercase; padding: .3rem .7rem; border-radius: 0 0 4px 4px;
  }
  .preis-label {
    font-size: .68rem; letter-spacing: .18em; text-transform: uppercase;
    color: var(--gold); font-weight: 600; margin-bottom: .75rem;
  }
  .preis-price {
    font-family: var(--serif); font-size: 2.6rem; font-weight: 700;
    color: var(--txt-hi); line-height: 1; margin-bottom: 1rem;
  }
  .preis-price span { font-size: 1rem; color: var(--txt-dim); font-family: var(--sans); font-weight: 400; }
  .preis-desc { font-size: .86rem; line-height: 1.65; color: var(--txt); margin-bottom: 1.5rem; }
  .preis-list { display: flex; flex-direction: column; gap: .45rem; margin-bottom: 2rem; flex: 1; }
  .preis-list li {
    font-size: .8rem; color: var(--txt-dim);
    display: flex; align-items: center; gap: .6rem;
  }
  .preis-list li::before { content: '—'; color: var(--gold); font-size: .7rem; flex-shrink: 0; }
  .preis-cta {
    display: inline-block; margin-top: auto;
    font-size: .78rem; font-weight: 700; letter-spacing: .06em;
    color: var(--txt); padding-bottom: 2px;
    border-bottom: 1px solid var(--bdr-hi); transition: color .2s, border-color .2s;
  }
  .preis-cta:hover { color: var(--gold); border-color: var(--gold); }
  .preis-cta-gold { color: var(--gold); border-color: var(--gold); }
  .preise-hint {
    text-align: center; margin-top: 2rem;
    font-size: .8rem; color: var(--txt-dim); letter-spacing: .03em;
  }

  /* ── STIMMEN ── */
  #stimmen { border-top: 1px solid var(--bdr); }
  .stimmen-grid { display: flex; justify-content: center; }
  .stimme-card {
    background: var(--bg-mid); border: 1px solid var(--bdr-hi); border-radius: 6px;
    padding: 5rem 6rem; max-width: 900px; width: 100%; position: relative;
    text-align: center;
  }
  .stimme-card::after {
    content: ''; position: absolute; bottom: 0; left: 0; right: 0; height: 3px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    border-radius: 0 0 6px 6px;
  }
  .stimme-quote {
    font-family: var(--serif); font-size: 8rem; line-height: .7;
    color: var(--gold); opacity: .3; margin-bottom: 1rem; display: block;
  }
  .stimme-card p {
    font-size: clamp(1.3rem, 2.5vw, 1.75rem); line-height: 1.65; color: var(--txt-hi);
    font-family: var(--serif); font-style: italic; margin-bottom: 2.5rem;
    font-weight: 400;
  }
  .stimme-card footer { display: flex; flex-direction: column; align-items: center; gap: .3rem; }
  .stimme-card footer strong { font-size: 1rem; color: var(--txt-hi); font-family: var(--sans); }
  .stimme-card footer span { font-size: .8rem; color: var(--txt-dim); letter-spacing: .08em; text-transform: uppercase; }

  /* ── FAQ ── */
  #faq { background: var(--bg-mid); border-top: 1px solid var(--bdr); }
  .faq-list { display: flex; flex-direction: column; border: 1px solid var(--bdr); border-radius: 4px; overflow: hidden; }
  .faq-item { border-bottom: 1px solid var(--bdr); }
  .faq-item:last-child { border-bottom: none; }
  .faq-item summary {
    padding: 1.4rem 1.75rem; cursor: pointer; list-style: none;
    font-size: 1rem; font-weight: 600; color: var(--txt-hi);
    display: flex; justify-content: space-between; align-items: center;
    gap: 1rem; transition: background .2s;
  }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-item summary::after {
    content: '+'; color: var(--gold); font-size: 1.4rem; font-weight: 300;
    flex-shrink: 0; transition: transform .25s;
  }
  .faq-item[open] summary { background: rgba(200,169,110,.04); }
  .faq-item[open] summary::after { transform: rotate(45deg); }
  .faq-answer { padding: 0 1.75rem 1.4rem; }
  .faq-answer p { font-size: .92rem; line-height: 1.75; color: var(--txt); }

  /* ── BLOG VORSCHAU ── */
  #blog-vorschau { border-top: 1px solid var(--bdr); }
  .blog-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; }
  .blog-card {
    background: var(--bg-mid); border: 1px solid var(--bdr); border-radius: 4px;
    padding: 2rem; display: flex; flex-direction: column; gap: .75rem;
    transition: border-color .3s, transform .25s;
  }
  .blog-card:hover { border-color: var(--bdr-hi); transform: translateY(-3px); }
  .blog-date { font-size: .68rem; letter-spacing: .1em; text-transform: uppercase; color: var(--gold); }
  .blog-card h3 {
    font-family: var(--serif); font-size: 1.2rem; font-weight: 600;
    color: var(--txt-hi); line-height: 1.3;
  }
  .blog-card p { font-size: .86rem; line-height: 1.65; color: var(--txt); flex: 1; }
  .blog-more { font-size: .8rem; font-weight: 600; color: var(--gold); margin-top: .5rem; }
  .blog-all { display: flex; justify-content: center; margin-top: 2.5rem; }

  /* ── FADE-UP ANIMATIONS ── */
  .fade-up {
    opacity: 0; transform: translateY(28px);
    transition: opacity .75s ease, transform .75s ease;
  }
  .fade-up.visible { opacity: 1; transform: translateY(0); }

  /* ── RESPONSIVE ── */
  @media (max-width: 1024px) {
    .warum-wrap { grid-template-columns: 1fr; }
    .warum-visual { max-width: 480px; }
    .results-grid { grid-template-columns: 1fr 1fr; }
    .prozess-grid { grid-template-columns: 1fr 1fr; border-right: 1px solid var(--bdr); }
    .pstep:nth-child(2), .pstep:nth-child(4) { border-right: 1px solid var(--bdr); }
    .pstep:nth-child(1), .pstep:nth-child(2) { border-bottom: 1px solid var(--bdr); }
    .preise-grid { grid-template-columns: 1fr 1fr; }
    .preise-grid .preis-card:last-child { grid-column: 1 / -1; }
    .blog-grid { grid-template-columns: 1fr 1fr; }
  }

  @media (max-width: 768px) {
    .nav-links, .nav-cta { display: none; }
    .nav-burger { display: flex; }
    .hero-headline { font-size: clamp(3.5rem, 14vw, 5.5rem); }
    .stat-div { display: none; }
    .stats-inner { display: grid; grid-template-columns: 1fr 1fr; }
    .section-header { flex-direction: column; gap: 1rem; }
    .sec-num { font-size: 3rem; }
    .services-grid { grid-template-columns: 1fr; }
    .prozess-grid { grid-template-columns: 1fr; border-right: 1px solid var(--bdr); }
    .pstep { border-right: 1px solid var(--bdr); border-bottom: 1px solid var(--bdr); }
    .results-grid { grid-template-columns: 1fr; }
    .preise-grid { grid-template-columns: 1fr; }
    .preise-grid .preis-card:last-child { grid-column: auto; }
    .blog-grid { grid-template-columns: 1fr; }
    .stimme-card { padding: 2.5rem 2rem; }
    .form-row { grid-template-columns: 1fr; }
    .footer-wrap { grid-template-columns: 1fr; gap: 2.5rem; }
    .footer-links { grid-template-columns: 1fr 1fr; }
    .warum-wrap { gap: 3rem; }
    .warum-visual { max-width: 100%; }
  }

  @media (max-width: 480px) {
    .hero-headline { font-size: clamp(3rem, 16vw, 4.5rem); }
    .stat { padding: 1.25rem 1.5rem; }
    .stats-inner { grid-template-columns: 1fr 1fr; gap: 0; }
    .res-metrics { flex-direction: column; gap: .75rem; }
    .res-metric { text-align: left; }
  }
</style>
</body>
</html>
